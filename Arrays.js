// Given an array of strings, use a function to reverse all the elements in the string in ascending order and the specific elements in descending order
function sorting(){
let names = ["Kamau","Maina","Rebecca","Anita","Joy"];
console.log(names.sort());
console.log(names.reverse());
}
sorting()






// Using JS functions and an array of numbers, return positive if an element within the array is positive, negative if an element is negative, else zero
function elementCheck(){
let number= number.forEach(element => {
    if (element >0){
        console.log("positive");
    }
    else if (element < 0){
        console.log("negative");
    }else{
        console.log("zero");
    }
});

var numbers = [4,5,7,8]
var numberif
}




